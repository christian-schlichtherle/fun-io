before_cache:
- find $HOME/.sbt -name "*.lock" | xargs rm
- find $HOME/.ivy2 -name "ivydata-*.properties" | xargs rm

cache:
  directories:
  - $HOME/.ivy2/cache
  - $HOME/.sbt

jdk:
- openjdk8
- openjdk9
- openjdk10
- openjdk11

language: scala

script: sbt +test
